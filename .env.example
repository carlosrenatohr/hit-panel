# InsForge backend (public values — the anon key is safe to ship to the browser).
# Copy to .env and fill in. For Cloudflare Pages, set these as build-time env vars too.
PUBLIC_INSFORGE_URL=https://a4qvtp8s.us-east.insforge.app
PUBLIC_INSFORGE_ANON_KEY=eyJ...your-anon-key...
