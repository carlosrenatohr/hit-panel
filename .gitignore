# build output
dist/
.astro/

# deps
node_modules/

# env & secrets
.env
.env.local
.env*.local
*.local.txt
ADMIN-CREDENTIALS.local.txt

# os/editor
.DS_Store
*.log

# Claude Code worktrees / local
.claude/


# graphify knowledge-graph output — keep the portable artifacts (graph.json + report)
# for cross-model handoff; ignore the rest (cache, html, temp, manifests).
graphify-out/*
!graphify-out/graph.json
!graphify-out/GRAPH_REPORT.md
